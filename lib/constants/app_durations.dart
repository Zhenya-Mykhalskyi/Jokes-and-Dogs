class AppDurations {
  AppDurations._();

  static const Duration shortAnimationDuration = Duration(milliseconds: 1000);
  static const Duration longAnimationDuration = Duration(milliseconds: 3000);
  static const Duration jokePunchlineDelay = Duration(milliseconds: 4000);
}
